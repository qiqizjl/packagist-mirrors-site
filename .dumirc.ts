import { defineConfig } from 'dumi';

export default defineConfig({
  themeConfig: {
    name: 'Packagist Mirrors',
    prefersColor:{
      default:"auto",
      switch:false
    },
    footer:"Powered by NaiXiaoXin Inc."
  },
});
